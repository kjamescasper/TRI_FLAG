# loop/__init__.py
# Package marker — makes loop/ importable as a Python package.
# Week 10: enables ACEGEN to import triflag_scorer by dotted path.
# Usage: from loop.triflag_scorer import triflag_score